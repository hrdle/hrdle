{
  "$comment": "Single source of truth for everything that names this product. Renaming the product (see #459) should be a change to this file plus the display strings, not a sweep of 1200 literals. install.sh and .github/workflows/release.yml cannot read it — the installer runs via `curl | bash` with no checkout, and Actions matrix values must be literals — so they keep their own copies, guarded by backend/tests/unit/identity-consistency.test.ts. defaultPort is 5924 only while Hrdle runs alongside the cchub install it replaces; promoting Hrdle means moving it back to 5923.",
  "productName": "Hrdle",
  "tagline": "Coding Agent Session Manager",
  "binaryName": "hrdle",
  "repo": "hrdle/hrdle",
  "assetPrefix": "hrdle",
  "defaultPort": 5924,
  "devPort": 3456,
  "dataDirName": ".hrdle",
  "dataDirEnv": "HRDLE_DATA_DIR",
  "configDirName": "hrdle",
  "serviceName": "hrdle",
  "launchdPrefix": "com.hrdle",
  "storagePrefix": "hrdle-",
  "legacyStoragePrefixes": [
    "cchub-",
    "cc-hub-"
  ],
  "tmpPrefix": "hrdle",
  "browserLogName": "hrdle-browser.log",
  "keychainService": "hrdle"
}
